从库已经有相关的数据了，导致同步异常，解决办法如下：

```shell
stop slave;

set @@session.gtid_next='xxxx(报错的地方的gtid)';

# 手动提交一个事务，跳过该事务
begin;
commit;

start slave;
```

从库没有拿到主库的最新数据，导致同步异常，解决办法如下：重新dump主库数据，导入到从库，重新建立主从关系。
清除binlog日志的命令： `purge binary logs to 'binlog.0000xx'`，将xx替换成最新的日志文件号。
