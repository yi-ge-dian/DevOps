切分一个 tar 包，按照 3GB 的块大小进行切分, 生成文件名是package.tar.gz.00, package.tar.gz.01, package.tar.gz.02, ...

```shell
split -b 3G xx.tar.gz -d xx.tar.gz.part_ --verbose
```

合并

```shell
cat xx.tar.gz.part_* >xx.tar.gz
```