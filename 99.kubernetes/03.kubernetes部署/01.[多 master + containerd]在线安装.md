# 1. 节点拓扑规划

```shell
cat /etc/os-release
uname -a
```

如下：


| 角色        | ip         | OS                | 配置             |
| ----------- | ---------- | ----------------- | ---------------- |
| k8s-master1 | 10.0.0.161 | Centos7.9, x86-64 | mem:4G,cpu:2,80G |
| k8s-master2 | 10.0.0.162 | Centos7.9, x86-64 | mem:4G,cpu:2,80G |
| K8s-master3 | 10.0.0.163 | Centos7.9, x86-64 | mem:4G,cpu:2,80G |
| K8s-node1   | 10.0.0.164 | Centos7.9, x86-64 | mem:4G,cpu:2,80G |

# 2. 版本

| 工具       | 版本   |
| ---------- | ------ |
| Containerd | 1.6.24 |
| k8s        | 1.26.9 |

# 3. 准备工作

## 3.1. 主机域名解析

```shell
######################################## master1 执行
hostnamectl set-hostname k8s-master1
######################################## master2 执行
hostnamectl set-hostname k8s-master2
######################################## master3 执行
hostnamectl set-hostname k8s-master3
######################################## node1 执行
hostnamectl set-hostname k8s-node1
######################################## 所有节点执行
cat >> /etc/hosts << EOF
10.0.0.161 k8s-master1
10.0.0.162 k8s-master2
10.0.0.163 k8s-master3
10.0.0.164 k8s-node1
EOF
```

## 3.2. 关闭防火墙

```shell
######################################## 所有节点执行
systemctl status firewalld
systemctl stop firewalld && systemctl disable firewalld
systemctl status firewalld

iptables -L # 查看
iptables -F # 清空
iptables -L # 查看
```

## 3.3. 关闭 SELinux

```shell
######################################## 所有节点执行

# 临时
setenforce 0
# 永久
setenforce 0 && sed -i 's/SELINUX=enforcing/SELINUX=disabled/' /etc/selinux/config

getenforce # 查看
```

## 3.4. 关闭 swap

```shell
######################################## 所有节点执行
# 查看
free -h

# 临时关闭
swapoff -a

# 永久
swapoff -a && sed -ri 's/.*swap.*/#&/' /etc/fstab
```

## 3.5. 时间同步

```shell
######################################## 所有节点执行
yum install -y chrony
timedatectl set-timezone Asia/Shanghai
```

### 3.5.1. 配置时钟主服务器

```shell
######################################## 时钟主服务器

# 让节点能自己从外网同步时间，然后把时间提供给内网其它服务器
# 如果该节点不能向外网同步时间，手动校验时间
# 调整时间，默认慢了12s
date -s "$(date -d '+12 seconds' '+%Y-%m-%d %H:%M:%S')"
hwclock --systohc

vim /etc/chrony.conf
# ...
server ntp1.aliyun.com iburst
server ntp2.aliyun.com iburst
server ntp3.aliyun.com iburst
server ntp4.aliyun.com iburst
# ...
allow 172.16.1.0/24
# ...

systemctl enable --now chronyd

# 验证同步是否成功，如果有外网，它应该会显示外面的时间源
chronyc sources -v
```

### 3.5.2. 配置时钟从服务器

```shell
######################################## 时钟从服务器

vim /etc/chrony.conf
# 指向主服务器 S1
server 172.16.1.15 iburst prefer
server ntp1.aliyun.com iburst
server ntp2.aliyun.com iburst
server ntp3.aliyun.com iburst
server ntp4.aliyun.com iburst

systemctl enable --now chronyd
# 验证同步是否成功，应该能看到主服务器的 IP 地址（172.16.1.15），前面有 ^* 标志，说明它被选作时间源
```

## 3.6. 修改内核参数

```shell
######################################## 所有节点执行
# 内核转发
cat > /etc/modules-load.d/k8s.conf
overlay
br_netfilter
EOF
modprobe overlay
modprobe br_netfilter

# 添加网桥过滤
cat > /etc/sysctl.d/k8s.conf << EOF
net.ipv4.ip_forward = 1
net.bridge.bridge-nf-call-iptables = 1
net.bridge.bridge-nf-call-ip6tables = 1
EOF
sysctl --system

# 查看
lsmod | grep br_netfilter
lsmod | grep overlay
sysctl net.bridge.bridge-nf-call-iptables net.bridge.bridge-nf-call-ip6tables net.ipv4.ip_forward
```

## 3.7. ipvs转发

```shell
######################################## 所有节点执行
yum install -y ipset ipvsadm

cat > /etc/sysconfig/modules/ipvs.modules << EOF
modprobe -- ip_vs
modprobe -- ip_vs_rr
modprobe -- ip_vs_wrr
modprobe -- ip_vs_sh
modprobe -- nf_conntrack
EOF

chmod 755 /etc/sysconfig/modules/ipvs.modules && bash /etc/sysconfig/modules/ipvs.modules && lsmod | grep -e ip_vs -e nf_conntrack
```

# 4. 安装 Containerd

```shell
######################################## 所有节点执行
# 查看是否有 runc, 判断 runc 是不是系统自带的，不要自带的，可以 mv 掉
which runc

# 1. 下载
curl -LO https://github.com/containerd/containerd/releases/download/v1.6.24/cri-containerd-cni-1.6.24-linux-amd64.tar.gz

tar -zxvf cri-containerd-cni-1.6.24-linux-amd64.tar.gz -C /
containerd -version

mkdir /etc/containerd
containerd config default > /etc/containerd/config.toml

vim /etc/containerd/config.toml

# 1.存储数据目录修改
root = "/data/containerd"
# 2.sandbox 修改
默认值：sandbox_image = “registry.k8s.io/pause:3.6”
目标值：sandbox_image = “registry.aliyuncs.com/google_containers/pause:3.9”
# 3. 驱动
SystemdCgroup改为true
# 4. 镜像加速
[plugins."io.containerd.grpc.v1.cri".registry.mirrors]
        [plugins."io.containerd.grpc.v1.cri".registry.mirrors."docker.io"]
          # 注意修改自己的镜像加速器地址
          endpoint = ["https://***.mirror.aliyuncs.com"]
        [plugins."io.containerd.grpc.v1.cri".registry.mirrors."k8s.gcr.io"]
          endpoint = ["https://registry.aliyuncs.com/google_containers"]
```

# 5. 安装 runc

```shell
######################################## 所有节点执行
# 1. 默认自带 runc 缺少依赖
runc
runc:symbol lookup error: runc: undefined symbol: seccomp_notify_respond
# 2. 下载依赖
curl -LO https://github.com/opencontainers/runc/releases/download/v1.1.9/libseccomp-2.5.4.tar.gz
# 3. 编译安装
yum install -y gcc gperf
tar -zxvf libseccomp-2.5.4.tar.gz
cd libseccomp-2.5.4
./configure
make && make install
# 4. 使用配套的 runc
curl -LO https://github.com/opencontainers/runc/releases/download/v1.1.9/runc.amd64
rm -rf /usr/local/sbin/runc
chmod +x runc.amd64
mv runc.amd64 /usr/local/sbin/runc
# 5. 启动 containerd
systemctl enable --now containerd
```

# 6. 负载均衡搭建

## 6.1. keepalived

```shell
######################################## 所有主节点执行
yum install -y keepalived
cp /etc/keepalived/keepalived.conf /etc/keepalived/keepalived.conf.bak
vim /etc/keepalived/keepalived.conf

# k8s_master_1
! Configuration File for keepalived
global_defs {
    router_id k8s_master_1
}
vrrp_script check_apiserver {
  script "/etc/keepalived/check_apiserver.sh"
  interval 3
  weight -30
  fall 10
  rise 2
}

vrrp_instance VI_1 {
    state MASTER
    interface ens33
    virtual_router_id 160
    priority 100
    authentication {
        auth_type PASS
        auth_pass 123456
    }
    virtual_ipaddress {
        192.168.1.160
    }
    track_script {
        check_apiserver
    }
}


# k8s_master_2
! Configuration File for keepalived
global_defs {
    router_id k8s_master_2
}
vrrp_script check_apiserver {
  script "/etc/keepalived/check_apiserver.sh"
  interval 3
  weight -30
  fall 10
  rise 2
}

vrrp_instance VI_1 {
    state MASTER
    interface ens33
    virtual_router_id 160
    priority 90
    authentication {
        auth_type PASS
        auth_pass 123456
    }
    virtual_ipaddress {
        192.168.1.160
    }
    track_script {
        check_apiserver
    }
}

# k8s_master_3
! Configuration File for keepalived
global_defs {
    router_id k8s_master_3
}
vrrp_script check_apiserver {
  script "/etc/keepalived/check_apiserver.sh"
  interval 3
  weight -30
  fall 10
  rise 2
}

vrrp_instance VI_1 {
    state MASTER
    interface ens33
    virtual_router_id 160
    priority 70
    authentication {
        auth_type PASS
        auth_pass 123456
    }
    virtual_ipaddress {
        192.168.1.160
    }
    track_script {
        check_apiserver
    }
}

vim /etc/keepalived/check_apiserver.sh
#!/bin/sh

errorExit() {
    echo "*** $*" 1>&2
    exit 1
}

curl --silent --max-time 2 --insecure https://localhost:6553/ -o /dev/null || errorExit "Error GET https://localhost:6553/"
if ip addr | grep -q 192.168.1.160; then
    curl --silent --max-time 2 --insecure https://192.168.1.160:6553/ -o /dev/null || errorExit "Error GET https://192.168.1.160:6553/"
fi

chmod +x /etc/keepalived/check_apiserver.sh
```

## 6.2. haproxy

```shell
######################################## 所有主节点执行
yum install -y haproxy
cp -a /etc/haproxy/haproxy.cfg /etc/haproxy/haproxy.cfg.bak
vim /etc/haproxy/haproxy.cfg

# /etc/haproxy/haproxy.cfg
#---------------------------------------------------------------------
# Global settings
#---------------------------------------------------------------------
global
    log /dev/log local0
    log /dev/log local1 notice
    daemon

#---------------------------------------------------------------------
# common defaults that all the 'listen' and 'backend' sections will
# use if not designated in their block
#---------------------------------------------------------------------
defaults
    mode                    http
    log                     global
    option                  httplog
    option                  dontlognull
    option http-server-close
    option forwardfor       except 127.0.0.0/8
    option                  redispatch
    retries                 1
    timeout http-request    10s
    timeout queue           20s
    timeout connect         5s
    timeout client          20s
    timeout server          20s
    timeout http-keep-alive 10s
    timeout check           10s

#---------------------------------------------------------------------
# apiserver frontend which proxys to the control plane nodes
#---------------------------------------------------------------------
frontend apiserver
    # 注意负载均衡的端口要与keepalived里面的配置保持一致
    bind *:6553
    mode tcp
    option tcplog
    default_backend apiserver

#---------------------------------------------------------------------
# round robin balancing for apiserver
#---------------------------------------------------------------------
backend apiserver
    option httpchk GET /healthz
    http-check expect status 200
    mode tcp
    option ssl-hello-chk
    balance     roundrobin
        # 多个主节点直接拼接到后面即可
        server k8s-master01 192.168.1.161:6443 check
        server k8s-master02 192.168.1.162:6443 check
        server k8s-master02 192.168.1.163:6443 check
```

# 7. 安装 k8s

## 7.1. 安装 kubeadm、kubectl、kubelet

```shell
######################################## 所有主节点执行
cat  > /etc/yum.repos.d/kubernetes.repo <<EOF
[kubernetes]
name=Kubernetes
baseurl=https://mirrors.aliyun.com/kubernetes/yum/repos/kubernetes-el7-x86_64/
enabled=1
gpgcheck=0
repo_gpgcheck=0
gpgkey=https://mirrors.aliyun.com/kubernetes/yum/doc/yum-key.gpg https://mirrors.aliyun.com/kubernetes/yum/doc/rpm-package-key.gpg
EOF
yum install -y kubeadm-1.26.9 kubectl-1.26.9 kubelet-1.26.9
```

## 7.2. 初始化 k8s集群

```shell
# k8s-master1 执行
kubeadm init \
--apiserver-advertise-address=192.168.1.161 \
--control-plane-endpoint "192.168.1.160:6553" \
--upload-certs \
--image-repository=registry.aliyuncs.com/google_containers \
--kubernetes-version=v1.26.9 \
--service-cidr=10.96.0.0/12 \
--pod-network-cidr=10.244.0.0/16 \
--cri-socket=unix:///var/run/containerd/containerd.sock

mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config

export KUBECONFIG=/etc/kubernetes/admin.conf
echo "export KUBECONFIG=/etc/kubernetes/admin.conf" >> /etc/profile
source /etc/profile

yum install -y bash-completion
echo 'source <(kubectl completion bash)' >> ~/.bashrc
source ~/.bashrc

# 记住加入主节点的命令
# 记住加入从节点的命令
```

## 7.3. 主节点加入集群

```shell
# k8s master2 加入集群
kubeadm join 192.168.1.160:6553 --token jxbuxf.z9e5jytyl8ipdrar \
--discovery-token-ca-cert-hash sha256:563acac93d6a44bacca801cf34a04c2874d95cd4d92eb42e49d5304130c90d84 \
--control-plane --certificate-key 8aaa559e0d5a3b81ec03045bd395b453cb2cfe9df2330a50484777a7987c9193 \
--cri-socket=unix:///var/run/containerd/containerd.sock

mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config

export KUBECONFIG=/etc/kubernetes/admin.conf
echo "export KUBECONFIG=/etc/kubernetes/admin.conf" >> /etc/profile
source /etc/profile

yum install -y bash-completion
echo 'source <(kubectl completion bash)' >> ~/.bashrc
source ~/.bashrc

# k8s master3 加入集群
kubeadm join 192.168.1.160:6553 --token jxbuxf.z9e5jytyl8ipdrar \
--discovery-token-ca-cert-hash sha256:563acac93d6a44bacca801cf34a04c2874d95cd4d92eb42e49d5304130c90d84 \
--control-plane --certificate-key 8aaa559e0d5a3b81ec03045bd395b453cb2cfe9df2330a50484777a7987c9193 \
--cri-socket=unix:///var/run/containerd/containerd.sock

mkdir -p $HOME/.kube
sudo cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
sudo chown $(id -u):$(id -g) $HOME/.kube/config

export KUBECONFIG=/etc/kubernetes/admin.conf
echo "export KUBECONFIG=/etc/kubernetes/admin.conf" >> /etc/profile
source /etc/profile

yum install -y bash-completion
echo 'source <(kubectl completion bash)' >> ~/.bashrc
source ~/.bashrc
```

> 1. 忘记命令，生成新的 token 和 join 命令
>
> kubeadm token create --print-join-command
>
> 2. 证书过期，重新上传证书并生成 certificate-key
>
> kubeadm init phase upload-certs --upload-certs

## 7.4. 加入工作节点

```shell
# k8s node 节点执行
kubeadm join 192.168.1.160:6553 --token jxbuxf.z9e5jytyl8ipdrar \
--discovery-token-ca-cert-hash sha256:563acac93d6a44bacca801cf34a04c2874d95cd4d92eb42e49d5304130c90d84 \
--cri-socket=unix:///var/run/containerd/containerd.sock
```

## 7.5. 配置 kubelet 启动

```shell
# 所有节点执行
systemctl enable kubelet
# 所有主节点执行
kubectl get nodes
```

# 8. 安装 calico 网络插件

https://docs.tigera.io/calico/latest/getting-started/kubernetes/quickstart

```shell
# 任意主节点执行
curl -LO https://raw.githubusercontent.com/projectcalico/calico/v3.26.3/manifests/tigera-operator.yaml
# 不要用 apply，为啥去了解apply 和 create 区别
kubectl create -f tigera-operator.yaml
kubectl get all -o wide -n tigera-operator

curl -LO https://raw.githubusercontent.com/projectcalico/calico/v3.26.3/manifests/custom-resources.yaml
sed -i 's/cidr: 192.168.0.0/cidr: 10.244.0.0/g' custom-resources.yaml
kubectl create -f custom-resources.yaml
```

# 9. 查看集群状态

```shell
kubectl get pods -A
kubectl get nodes
```

